Configuration loader
====================

TOML config loader. Merges the user's ``sphot_config.toml`` with the bundled ``default_config.toml``. See :doc:`../rst/config` for the parameter reference.

.. automodule:: sphot.config
